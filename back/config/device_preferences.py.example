"""
장치 우선순위 설정 예시 파일.

이 파일을 복사하여 `config/device_preferences.py`로 저장한 뒤
환경에 맞게 우선순위를 수정하세요. 실제 설정 파일은 gitignore에 포함해
개인 장치 구성을 안전하게 유지할 수 있습니다.
"""

# 마이크 우선순위 (부분 문자열로 매칭됩니다)
MICROPHONE_PRIORITY_NAMES = [
    # "USB Mic",
    # "MacBook Pro Microphone",
]

# 스피커 우선순위 (부분 문자열로 매칭됩니다)
SPEAKER_PRIORITY_NAMES = [
    # "HD Audio",
    # "External Speaker",
]

# 특정 카메라 소스를 강제로 사용할 경우 설정 (예: "/dev/video0" 또는 0)
# CAMERA_SOURCE = "/dev/video0"
